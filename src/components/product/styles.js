import styled from 'styled-components';

const StyledProduct = styled.div`
	display: flex;
	flex-direction: column;
	max-width: 200px;
`;

const StyledImage = styled.img`
	width: 150px;
	border-radius: 0.5rem;
`;

export { StyledImage, StyledProduct };
